1- MATRIZ + MALLOC
Adição de matrizes: Escreva uma função em C que receba duas matrizes como parâmetros, aloque dinamicamente memória para uma terceira matriz que armazenará a soma das duas matrizes e retorne um ponteiro para a matriz resultante.
Multiplicação de matrizes: Escreva uma função em C que realize a multiplicação de duas matrizes dinamicamente alocadas, retornando um ponteiro para a matriz resultante.

Transposição de matriz: Implemente uma função que receba uma matriz como argumento, aloque dinamicamente memória para a matriz transposta e retorne um ponteiro para a matriz transposta.
Verificação de simetria: Escreva uma função que verifique se uma matriz quadrada é simétrica. A função deve aceitar uma matriz dinamicamente alocada como argumento.

Inversão de matriz: Implemente uma função que inverta uma matriz quadrada. A função deve aceitar uma matriz dinamicamente alocada como entrada e retornar um ponteiro para a matriz invertida.
Cálculo da diagonal principal: Escreva uma função que receba uma matriz quadrada e retorne um array dinamicamente alocado contendo os elementos da diagonal principal.

Verificação de matriz identidade: Implemente uma função que verifique se uma matriz quadrada é uma matriz identidade. A função deve aceitar uma matriz dinamicamente alocada como argumento.

2- MATRIZ + MALLOC + HEADER

Soma de Matrizes: Crie uma função em um arquivo de header (.h) que realize a soma de duas matrizes dinamicamente alocadas e retorne a matriz resultante.

Multiplicação de Matrizes: Escreva uma função em um arquivo de header (.h) que multiplique duas matrizes dinamicamente alocadas e retorne a matriz resultante.

Transposição de Matriz: Implemente uma função em um arquivo de header (.h) que receba uma matriz dinamicamente alocada como entrada e retorne a matriz transposta.

Verificação de Simetria: Crie uma função em um arquivo de header (.h) que verifique se uma matriz quadrada é simétrica. A função deve aceitar uma matriz dinamicamente alocada como argumento.

Inversão de Matriz: Escreva uma função em um arquivo de header (.h) que inverta uma matriz quadrada dinamicamente alocada. A função deve retornar um ponteiro para a matriz invertida.

Cálculo da Diagonal Principal: Implemente uma função em um arquivo de header (.h) que receba uma matriz quadrada dinamicamente alocada e retorne um array dinamicamente alocado contendo os elementos da diagonal principal.

3- RECURSIVAS

Fatorial: Escreva uma função recursiva para calcular o fatorial de um número inteiro não negativo.

Fibonacci: Implemente uma função recursiva para gerar o n-ésimo termo da sequência de Fibonacci.

Potência: Crie uma função recursiva para calcular a potência de um número inteiro base elevado a um expoente não negativo.
Soma dos dígitos: Escreva uma função recursiva para calcular a soma dos dígitos de um número inteiro.

Torre de Hanoi: Implemente uma função recursiva para resolver o problema da Torre de Hanoi para mover uma torre de discos de um pino para outro, usando um terceiro pino como auxiliar.

Busca Binária Recursiva: Escreva uma função recursiva para realizar uma busca binária em um array ordenado.

4 - PONTEIRO COMO PARÂMETROD DE FUNÇÃO

Troca de Valores: Escreva uma função que receba dois ponteiros para inteiros como parâmetros e troque os valores apontados por esses ponteiros.

Cópia de String: Implemente uma função que receba um ponteiro para uma string de origem e um ponteiro para uma string de destino como parâmetros, e copie a string de origem para a string de destino.

Ordenação de Array: Crie uma função que ordene um array de inteiros usando o algoritmo de ordenação de sua escolha. A função deve receber um ponteiro para o array e o tamanho do array como parâmetros.

Soma dos Elementos de um Array: Escreva uma função que calcule a soma de todos os elementos de um array de inteiros. A função deve receber um ponteiro para o array e o tamanho do array como parâmetros.

Matriz Transposta: Implemente uma função que calcule a transposta de uma matriz quadrada. A função deve receber um ponteiro para a matriz e sua ordem como parâmetros.

Concatenação de Strings: Escreva uma função que concatene duas strings em uma terceira string. A função deve receber ponteiros para as duas strings de entrada e um ponteiro para a string de saída como parâmetros.

5 - MALLOC + ARRAY+ HEADER


Criação de Array Dinâmico: Escreva uma função em um arquivo de header (.h) que aloque dinamicamente memória para um array de inteiros com um tamanho especificado e retorne um ponteiro para esse array.

Preenchimento de Array Dinâmico: Implemente uma função em um arquivo de header (.h) que preencha um array dinamicamente alocado com valores fornecidos como argumento.

Ordenação de Array Dinâmico: Crie uma função em um arquivo de header (.h) que ordene um array de inteiros dinamicamente alocado usando um algoritmo de ordenação de sua escolha.

Liberação de Memória de Array Dinâmico: Escreva uma função em um arquivo de header (.h) que libere a memória alocada dinamicamente para um array de inteiros.

Concatenação de Arrays Dinâmicos: Implemente uma função em um arquivo de header (.h) que concatene dois arrays dinamicamente alocados em um terceiro array dinamicamente alocado.

Busca em Array Dinâmico: Escreva uma função em um arquivo de header (.h) que realize uma busca por um valor em um array dinamicamente alocado e retorne a posição do valor, se encontrado, ou -1 caso contrário.
