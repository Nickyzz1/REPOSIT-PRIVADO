<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Cadastro Aluno</title>
</head>
<body style="background-color: rgba(211, 205, 205, 0.87);">
    <header>
        <div class="collapse" id="navbarToggleExternalContent">
            <div class="bg-dark p-4">
                <h5 class="text-white h4">Menu</h5>
                <h6 class="text-white h6">
                    <a class="link" href="/" target="_blank">Voltar</a> |  
                    <a class="link" href="#" target="_blank">Ajuda</a>
                </h6>
                <span class="text-muted">Toggleable via the navbar brand.</span>
            </div>
        </div>
        <nav class="navbar navbar-dark bg-dark">
            <div class="container-fluid">
                
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                
            </div>
        </nav>
    </header>
    
    <div class="container espaço" style="display: flex; flex-direction: column !important;">

        <h2>Editar informações do aluno</h2>
        
        <form action="/editarAluno/<%= alunos.IDAluno %>" method="post" enctype="multipart/form-data">
            
            <div class="max-width image-upload content" style="display: flex; flex-direction: column !important;">
                
                <div class="image-upload">
                    <label for="uploadImage">
                        <img class="img-perfil" src="/img/<%= alunos.Foto %>"  id="uploadPreview" name="imagex">
                    </label>  
                    <input id="uploadImage" type="file" accept="image/*" id="foto" name="foto" onchange="PreviewImageEdit();">
                </div>
            
            <div class="content" style="margin: 10% !important">
                <div class="user-box">
                    <label>Nome</label>
                    <input type="text" value="<%= alunos.Nome %>" name="nome" class="inputStyle">
                </div>
                <div class="user-box">
                    <label>Idade</label>
                    <input type="text" value="<%= alunos.Idade %>" name="idade" class="inputStyle">
                </div>
                <div class="user-box">
                    <label>Sexo</label>
                    <input type="text" value="<%= alunos.Sexo %>" name="sexo" class="inputStyle" style="margin-bottom: 10%;"  >
                </div>
                <div class="user-box">
                    <label>Sala</label>
                    <select id="sala" name="sala">
                        <% for (let i=0; i<salas.length; i++) { %>
                            <option value='<%= salas[i].IDSala %>' <%= alunos.IDSala == salas[i].IDSala ? 'selected' : ''%>> <%=
                                salas[i].Nome %> </option>
                                <% } %>
                            </select>
                        </div>
                        <input class="btn btn-primary" type="submit" value="Atualizar" style="margin-top: 10%;" />
                    </form>
                </div>
            </div>
        </div>
        
        <script src="js/script.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
        
    </body>
    </html>