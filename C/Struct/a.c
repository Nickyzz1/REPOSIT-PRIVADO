criao struct veiculo

veiculo *listaveiculo = null;


listaveiculos = addveiculo(listaveiculo, 2005)
display(listaveículos)


void displçay(veiculo *p)
{
if(p)
{

do
	print("%i",p->ano)

while(p)

else{
	print(lista vazia)


addVeiculo(veiculo *listaveiculo)
{





Enum e for each:
Crie um enum DiaDaSemana com os dias da semana. Em seguida, crie uma classe Main com um método que itere sobre todos os dias da semana usando um loop for each e imprima cada dia.

Herança com utilização do super:
Crie uma classe Animal com um método emitirSom(). Em seguida, crie duas subclasses Cachorro e Gato que estendem Animal e sobrescrevem o método emitirSom() para que cada um emita um som diferente. Utilize o super para chamar o método da superclasse.

Ternário:
Escreva um programa que compare dois números e imprima "Maior" se o primeiro número for maior que o segundo, "Menor" se o primeiro número for menor que o segundo, e "Iguais" caso contrário, utilizando o operador ternário.

Try-catch-finally:
Escreva um programa que peça ao usuário para digitar um número. Utilize um bloco try-catch para lidar com a exceção caso o usuário digite uma entrada inválida. No bloco finally, imprima uma mensagem indicando que o programa foi encerrado.

Hash e biblioteca Math:
Crie um programa que calcule o hash de uma string usando o método hashCode() e imprima o resultado. Em seguida, use a biblioteca Math para calcular a raiz quadrada do hash.

Uso de equals e operadores lógicos básicos:
Escreva um programa que compare duas strings utilizando o método equals() e imprima "Iguais" se forem iguais e "Diferentes" caso contrário. Em seguida, escreva uma condição que verifique se ambas as strings têm o mesmo tamanho e imprima "Tamanhos iguais" se verdadeiro e "Tamanhos diferentes" caso contrário.
